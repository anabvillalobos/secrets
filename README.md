# secrets

# This script stores API keys and access tokens needed for a Twitter bot project. It does not run the main bot itself, but provides the authentication information that another script can use to connect to the Twitter/X API.
